{
    "trailingSlash": "auto",
    "routes": [
        {
            "route": "/runtime/",
            "rewrite": "/fail",
            "redirect": "/fail2",
            "backendRewrite": "/runtime/"
        }
    ],
    "navigationFallback": {
        "rewrite": "/index.html"
    }
}