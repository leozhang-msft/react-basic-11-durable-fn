{
    "trailingSlash": "auto",
    "routes": [
        {
            "route": "/api/runtime/",
            "apiRewrite": "/runtime/"
        }
    ],
    "navigationFallback": {
        "rewrite": "/index.html"
    }
}