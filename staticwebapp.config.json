{
    "trailingSlash": "auto",
    "routes": [
        {
            "route": "/api/runtime/",
            "redirect": "/thisshouldbreak/",
            "backendRewrite": "/runtime/"
        }
    ],
    "navigationFallback": {
        "rewrite": "/index.html"
    }
}