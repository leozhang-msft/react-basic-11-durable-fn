{
    "trailingSlash": "auto",
    "routes": [
        {
            "route": "/api/runtime/",
            "redirect": "/thisshouldbreak/",
            "backendRewrite": "/runtime/"
        },
        {
            "route": "/runtime/",
            "backendRewrite": "/runtime/"
        }
    ],
    "navigationFallback": {
        "rewrite": "/index.html"
    }
}