{
    "trailingSlash": "auto",
    "routes": [
        {
            "route": "/api/runtime/",
            "backendRewrite": "/runtime/"
        },
        {
            "route": "/runtime/",
            "backendRewrite": "/runtime/"
        }
    ],
    "navigationFallback": {
        "rewrite": "/index.html"
    }
}