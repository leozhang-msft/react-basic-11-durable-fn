{
    "trailingSlash": "auto",
    "routes": [
        {
            "route": "/runtime/",
            "apiRewrite": "/runtime/"
        }
    ],
    "navigationFallback": {
        "rewrite": "/index.html"
    }
}